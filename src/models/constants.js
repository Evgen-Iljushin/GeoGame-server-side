'use strict';

const REWARDS = {
  MONEY: 'money',
  COUPONS: 'coupons',
  COUPONS_PARTS: 'coupons_parts',
  SOFT_CURRENCY: 'soft_currency',
  HARD_CURRENCY: 'hard_currency'
};

const REWARDS_ENUM = Object.values(REWARDS);


module.exports = {
  REWARDS,
  REWARDS_ENUM
};
